using System;

namespace Calculadora_de_altura
{
    class Program
    {
        static void Main(string[] args)
            //de pies y pulgadas a centimetros
        {
            //double pulgadas, pies, altura;

            // Console.WriteLine("Dime las pulgadas");
            // pulgadas = double.Parse(Console.ReadLine());

            // Console.WriteLine("Dime los pies");
            // pies = double.Parse(Console.ReadLine());

            // altura = ((pies  * 12 ) + pulgadas) * 2.54;

            // Console.WriteLine("tu altura es " + altura + "en centimetros");

            //como redondear 

            //double d = 68.9;

            //int i = (int)Math.Round(d);// (inT) transforma un double en un entero y Math.Round redondea un numero real.


            //de centimetros a pies y pulgadas
            // dividir cms entres 2.54
            // redondear y meter en un int
            // dividir (entera) entre 12
            // el resto son las pulgadas

            double cms, entera, pies, pulgadas;

            Console.WriteLine();













        }
    }
}
